# 卡牌插画目录

把 AI 生成的卡牌精灵图命名为 **cards.png** 放到本目录即可自动启用图片卡面。

要求：
- 一张大图，**7 列 × 9 行** 均匀网格，格子铺满整张图（无外边距）
- 62 个格子按 `data.js` 中 `ART_ORDER` 的顺序排列（行优先），最后 1 格留空
- 建议尺寸 ≥ 1792×2304（每格 ≥ 256px）

没有此文件时游戏自动使用表情符号占位，不影响游玩。
如果生成的网格不是 7×9，修改 `data.js` 顶部 `CARD_SPRITE` 的 cols/rows 即可。
